import {quran,surahIndex,verseSource} from '@/lib/quranexa/sources';
export async function GET(req:Request){const q=new URL(req.url).searchParams;if(!q.has('surah'))return Response.json({surahs:surahIndex},{headers:{'Cache-Control':'public,max-age=86400'}});const s=Number(q.get('surah')),a=q.has('ayah')?Number(q.get('ayah')):null;if(!Number.isInteger(s)||s<1||s>114)return Response.json({error:'invalid_reference'},{status:400});if(a!==null){const source=verseSource(s,a);return Response.json(source?{source}:{error:'invalid_reference'},{status:source?200:404})}const page=Math.max(1,Math.floor(Number(q.get('page'))||1));const surah=quran[s-1];return Response.json({surah:surahIndex[s-1],page,pages:Math.ceil(surah.total_verses/20),ayahs:surah.verses.slice((page-1)*20,page*20).map(v=>verseSource(s,v.id))},{headers:{'Cache-Control':'public,max-age=86400'}})}
