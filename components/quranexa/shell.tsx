'use client';
import Link from 'next/link';import {usePathname} from 'next/navigation';
import {BookOpen,Search,Globe2,UserRound,Menu,Bookmark,History,Settings,Sparkles,ArrowUpRight,LibraryBig} from 'lucide-react';
import {Sheet,SheetTrigger,SheetContent,SheetTitle,SheetClose} from '@/components/ui/sheet';
import {useQ} from './context';import {Choice} from './controls';import {languages,languageOptions} from '@/lib/quranexa/i18n';
export function Shell({children}:{children:React.ReactNode}){const {locale,d,signedIn}=useQ(),path=usePathname();const nav=[['quran',d.quran],['hadith',d.hadith],['duas',d.duas],['ask',d.ask]];return <><header className="site-header"><div className="header-inner"><Link href={'/'+locale} className="brand"><span className="brand-icon"><BookOpen size={25}/></span><span>{d.brand}</span></Link><nav className="desktop-nav" aria-label={d.home}>{nav.map(([p,t])=><Link key={p} href={`/${locale}/${p}`} className={path.includes('/'+p)?'active':''}>{p==='ask'&&<Sparkles size={15}/>} {t}</Link>)}</nav><div className="header-actions"><Link href={`/${locale}/search`} aria-label={d.search} className="icon-button"><Search size={20}/></Link><div className="language-picker"><Globe2 size={17}/><Choice label={d.interface} value={locale} onChange={l=>{window.location.href=path.replace(/^\/(en|ur|ar)/,'/'+l)+window.location.search}} options={languageOptions(locale)}/></div><Link href={`/${locale}/${signedIn?'profile':'login'}`} className="account-button"><UserRound size={17}/><span>{d.account}</span></Link><Sheet><SheetTrigger className="mobile-menu icon-button" aria-label={d.more}><Menu/></SheetTrigger><SheetContent side={locale==='en'?'right':'left'} className="mobile-sheet"><SheetTitle>{d.brand}</SheetTitle><nav>{[['',d.home],...nav,['kalmas',d.kalmas],['bookmarks',d.bookmarks],['history',d.history],['settings',d.settings]].map(([p,t])=><SheetClose asChild key={p}><Link href={`/${locale}/${p}`}>{t}</Link></SheetClose>)}</nav></SheetContent></Sheet></div></div></header><div className="utility-bar"><div><span><BookOpen size={14}/>{d.sourceNote}</span><nav><Link href={`/${locale}/bookmarks`}><Bookmark size={14}/>{d.bookmarks}</Link><Link href={`/${locale}/history`}><History size={14}/>{d.history}</Link><Link href={`/${locale}/settings`} aria-label={d.settings}><Settings size={15}/></Link></nav></div></div><main className="main-container">{children}</main><footer><div className="footer-top"><Link href={'/'+locale} className="brand"><BookOpen size={22}/>{d.brand}</Link><span>{d.tagline}</span><nav><Link href={`/${locale}/kalmas`}>{d.kalmas}</Link><Link href={`/${locale}/sources`}>{d.sourcePolicy}</Link><Link href={`/${locale}/privacy`}>{d.privacy}</Link></nav></div><div className="footer-bottom"><span>© {new Date().getFullYear()} {d.brand}</span><span>{d.sourcePrivacy}</span></div></footer></>}
